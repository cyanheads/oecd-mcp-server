/**
 * @fileoverview Barrel export for OECD MCP prompt definitions.
 * @module mcp-server/prompts
 *
 * The OECD server is data-oriented; no prompts are defined. Workflow guidance
 * belongs in tool descriptions per the design spec.
 */

export const allPromptDefinitions: never[] = [];
